# /components

Shared, reusable UI components for NavixAI.

## Structure (planned)

```
/components
├── ui/           # Primitive design-system components (Button, Badge, Card…)
├── map/          # Real-time vehicle map and threat overlay
├── fleet/        # Fleet table and vehicle status panels
└── layout/       # Navbar, Sidebar, Footer
```
